#ifndef MAIN_H
#define MAIN_H

/** C library functions used **/
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>

/** PROTOTYPES **/
int _putchar(char c);
int _printf(const char *format, ...);
int _putstr(char *str);

#endif
