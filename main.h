#ifndef MAIN.H
#define MAIN.H

/** C library functions used **/
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>

/** PROTOTYPES **/
int print_out(char c);
int _printf(const char *format, ...);
int print_str(char *str);

#endif
